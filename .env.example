# JWT Configuration
# Use the same key for local and Docker to ensure tokens are compatible
JWT_SECRET_KEY=dev-secret-key-for-local-development-only

# Database (for local development)
DATABASE_URL=postgresql://user:password@localhost:5432/financial_mcp

# Claude API (optional - leave empty to use mock orchestrator)
CLAUDE_API_KEY=

# Debug mode
DEBUG=True
